<%= form_with(model: survey_response_user) do |form| %>
  <% if survey_response_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(survey_response_user.errors.count, "error") %> prohibited this survey_response_user from being saved:</h2>

      <ul>
        <% survey_response_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :session_id %>
    <%= form.text_field :session_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :survey_response %>
    <%= form.text_field :survey_response %>
  </div>

  <div class="field">
    <%= form.label :survey_question %>
    <%= form.text_field :survey_question %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
